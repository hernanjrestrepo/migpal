# --- Core API ---
fastapi==0.115.6
uvicorn[standard]==0.30.6
pydantic
pydantic-settings==2.3.0
python-multipart==0.0.9
email-validator==2.2.0

# --- Database ---
sqlmodel
SQLAlchemy
alembic
aiosqlite==0.20.0
psycopg2-binary==2.9.9

# --- Cache / infra clients ---
redis==5.0.8

# --- Auth / security ---
passlib==1.7.4
bcrypt==4.1.3
python-jose[cryptography]==3.3.0
cryptography==42.0.5

# --- HTTP / scraping ---
# httpx 0.28 rompe starlette.testclient (conflicto de metaclase en
# WebSocketDenialResponse) hasta que starlette lo parchea -- 0.27.2 es la
# última versión probada compatible con fastapi 0.115.6 + starlette 0.41.3.
httpx==0.27.2
beautifulsoup4==4.12.3
python-dotenv==1.0.1

# --- Telegram bot ---
python-telegram-bot==21.7
apscheduler==3.10.4

# --- AI providers (ollama via httpx; SDKs below only used if AI_PROVIDER is switched) ---
openai==1.51.0
anthropic==0.34.2
google-generativeai==0.8.3

# --- PDF / reporting ---
fpdf2==2.7.9
reportlab==4.2.5

# --- Data / NLP helpers used by scoring & sentiment analysis ---
numpy==1.26.4
matplotlib==3.9.2
textblob==0.18.0.post0
vaderSentiment==3.3.2

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# --- Dev / quality tooling (Sprint 0 — Foundation) ---
ruff==0.7.4
black==24.10.0
mypy==1.13.0
pre-commit==4.0.1
